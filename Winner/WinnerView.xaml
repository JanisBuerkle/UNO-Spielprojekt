<UserControl x:Class="UNO_Spielprojekt.Winner.WinnerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" Name="Control">
    <Grid DataContext="{Binding ElementName=Control, Path=ViewModel}">

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <TextBlock Grid.Column="1" Grid.Row="0" Background="Gray" VerticalAlignment="Center"
                   HorizontalAlignment="Center" FontSize="80" FontWeight="Bold" Text="WINNER" />

        <StackPanel Grid.Row="1" Grid.Column="1">
            <Label FontSize="20" Foreground="White" 
                   DataContext="{Binding ElementName=Control, Path=GameViewModel}" Content="{Binding Winners}" />
            <Label FontSize="20" Foreground="White" Content="Thomas" />
        </StackPanel>

        <Button Grid.Row="0" Grid.Column="0" Command="{Binding GoToMainMenuCommand}"
                Style="{StaticResource HomeButtonStyle}" />
    </Grid>
</UserControl>